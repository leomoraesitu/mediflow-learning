name: checkout_domain
description: Pure Dart domain models and checkout rules for MediFlow.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.1

resolution: workspace

dependencies:
  uuid: ^4.6.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
