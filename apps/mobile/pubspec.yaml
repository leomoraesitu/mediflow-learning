name: mediflow_mobile
description: Aplicativo Flutter Android educacional do MediFlow.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  checkout_domain: ^0.1.0
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  bloc_test: ^10.0.0

flutter:
  uses-material-design: true
